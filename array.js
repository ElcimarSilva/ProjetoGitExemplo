let lista = []

oneItem = {name: 'test500'}
listOfItems = [{name: 'tasest'}, {name: 'astest2'}, {name: 'satest3'}]
let nameMission = Math.random().toString(36).slice(2)

// lista.push(oneItem)
lista.push(nameMission)

// lista.forEach((item) => {
//     if (item.name) console.log(item.name)
// })

if (lista) console.log(lista[0]);

// console.log('primeiro item: ',lista[0].name)